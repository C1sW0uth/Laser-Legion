#ifndef SYSTEM_H
#define SYSTEM_H

#include<bits/stdc++.h>

class System {
public:
    void printTutorial(); // Prints the tutorial instructions
    int getInputInRange(int a, int b); // Reads user input within the specified range [a, b]
};

#endif
